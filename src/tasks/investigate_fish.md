# Check how Fish are rendered
Fish are visible, so their rendering logic is correct.
I will compare `FishManager` (or Renderer) with `UnitRenderer`.
If Fish use `InstancedMesh` successfully, I will copy their setup.
If Fish use simple `Mesh`, it confirms `InstancedMesh` allows specific failure.
